DATA SEGMENT
SOURCE DB 'CHENBINBIN16340015',0DH,0AH,'$'
COUNT EQU $-SOURCE
DATA ENDS
EXTRA SEGMENT
DEST DB COUNT DUP (?)
EXTRA ENDS
CODE SEGMENT
   ASSUME CS:CODE,DS:DATA,ES:EXTRA
BEGIN:
   MOV AX,DATA
   MOV DS,AX
   MOV BX,EXTRA
   MOV ES,BX
   LEA SI,SOURCE
   LEA DI,DEST;
   MOV CX,COUNT
   REP MOVS DEST,SOURCE
   MOV DX,OFFSET DEST
   MOV AH,9
   INT 21H
   MOV AH,4CH
   INT 21H
CODE ENDS
   END BEGIN
